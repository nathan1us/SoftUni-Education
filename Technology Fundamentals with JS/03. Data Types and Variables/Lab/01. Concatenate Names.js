function concatenate(firstName, lastName, del) {
  console.log(`${firstName}${del}${lastName}`);
}

/*
concatenate('John', 'Smith', '->');
concatenate('Jan', 'White', '<->');
concatenate('Linda', 'Terry', '=>');
*/
